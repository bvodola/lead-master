<% if(parts.length > 0) { %>
  <% for(var i=0; i<parts.length ; i++) { %>

    <!-- ** -->
    <!-- PF -->
    <!-- ** -->
    <% if(parts[i].type == 'pf') { %>
      
      <%= parts[i].name %>, <%= parts[i].nacionality %>, <%= parts[i].marital_status %>,
      <%= parts[i].job %>, <%= parts[i].job_subtitle ? parts[i].job_subtitle + ',' : '' %>
      <%= parts[i].cpf ? 'CPF ' + parts[i].cpf + ',' : '' %>
      <%= parts[i].rg ? 'RG ' + parts[i].rg.number + ',' : '' %>
      <%= parts[i].email ? 'com e-mail ' + parts[i].email + ',' : '' %>

      <% if(typeof parts[i].show_address === 'undefined' || parts[i].show_address) { %>
        com endereço em
        <%= parts[i].address.street %>, <%= parts[i].address.number %>, <%= parts[i].address.neighborhood %>,
        <%= parts[i].address.city %>/<%= parts[i].address.state %>, CEP <%= parts[i].address.zip %>.
      <% } %>

    <% } %>
    <br>

  <% } %>
<% } else { %>
  <br>
  _____________________________________________________________________________________________________
<% } %>